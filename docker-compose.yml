version: '3.7'

services:
  database:
    image: 'postgres:latest'
    restart: always
    ports:
      - 5432:5432

    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: root1234
      POSTGRES_DB: test 